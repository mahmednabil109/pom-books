<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        body {
            background: url("background1.jpg") no-repeat center center;
            background-size: cover;
            height: 100vh;
            font-family: monospace;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
        }
        
        h1 {
            color: white;
            text-align: center;
            text-shadow: 0 2px 9px rgba(220, 220, 220, 0.6);
            -webkit-text-stroke: none;
        }
        
        input {
            border: none;
            outline: none;
            font-size: 20px;
            padding: 4px;
            border-radius: 5px;
        }
        
        .msg {
            width: 400px;
            height: 30px;
            position: absolute;
            background: #f51111;
            font: bold 20px Georgia, sans-serif;
            text-align: center;
            color: white;
            border-radius: 5px;
            box-shadow: 2px 2px 8px rgba(255, 255, 255, 0.9);
            animation: down .8s ease-in-out, up .6s ease-in-out 5s;
            animation-fill-mode: forwards
        }
        
        @keyframes down {
            from {
                top: -40px;
            }
            to {
                top: 10px;
            }
        }
        
        @keyframes up {
            from {
                top: 10px;
            }
            to {
                top: -40px;
            }
        }
        
        .container {
            width: 600px;
            height: 100px;
            margin: 12px;
        }
        
        .container h1 {
            text-align: center;
            font-size: 42px;
        }
        
        .cont {
            width: 600px;
            height: 600px;
            color: white;
            position: relative;
            margin-top: 12px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-around;
        }
        
        .cont::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: linear-gradient(to right bottom, #6b0b02a2, #7e300ba2, #904e16a2, #a06c25a2, #ae8a3aa2);
            box-shadow: 0px 3px 10px rgba(0, 0, 0, 0.9);
            filter: blur(5px);
        }
        
        .cont form {
            width: 100%;
            height: 100%;
            position: relative;
            font-size: 20px;
            font-weight: 900;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            border-radius: 5px;
            padding: 30px 10px;
        }
        
        .cont .wrap {
            height: 100px;
            display: flex;
            flex-direction: column;
            margin: 8px;
        }
        
        .cont .wrap input {
            height: 35px;
            width: 350px;
            margin-top: 4px;
            color: black;
            box-shadow: 1px 1px 5px rgba(0, 0, 0, 0.9);
        }
        
        .cont .wrap input:hover {
            cursor: pointer;
        }
        
        .cont .wrap input[type="submit"] {
            height: 45px;
            width: 130px;
            font-size: 20px;
            font-family: monospace;
            color: white;
            background-color: #333;
            align-self: center;
            margin-bottom: 12px;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Welcome</title>
</head>

<body>
    <% if(locals.msg){%>
        <div class="msg">
            <%= msg %>
        </div>
        <%}%>
            <div class="container">
                <h1>Welcome</h1>
            </div>

            <div class="cont">
                <form method="POST">
                    <div class="wrap">
                        <label for="user">Username</label>
                        <input id="user" type="text" name="username">
                    </div>
                    <div class="wrap">
                        <label for="pass">Password</label>
                        <input id="pass" type="password" name="password">
                    </div>
                    <div class="wrap">
                        <input type="submit" value="Login">
                        <a href="registration">I dont have an account</a>
                    </div>
                </form>
            </div>
</body>

</html>