# POM BOOKS SITE
> # Instructions

* To intstall the dependencies simply type
    ```bash
    npm i
    ```
* In case you have unix system you can run the server with
    ```bash
     make
    ```
* Or you can type
    ```bash
    npm run start
    ```
